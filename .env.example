# Local backend (default). For production, set your Render API URL.
VITE_API_URL=http://localhost:5000
# VITE_API_URL=https://your-service.onrender.com

# Public frontend URL (no trailing slash). Used for canonical URLs, Open Graph, and sitemap generation.
# VITE_SITE_URL=https://your-app.vercel.app
